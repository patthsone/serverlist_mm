"cfg"
{
	// Seconds between chat adverts of a server (one server per advert, round robin). 0 = off.
	"annonce_interval"	"90.0"

	// Seconds between server queries (A2S over UDP). The menu shows the result of the last query.
	"update_interval"	"20.0"

	// Not used by this build (it queries the servers directly, no Steam Web API). Kept so an existing cfg loads as is.
	"apikey"	""

	"servers"
	{
		// The block name is what players see in the list; "ip" is host:port and is what "connect" gets.
		"Server #FF2"
		{
			"ip"	"45.136.204.250:27015"
		}
		"Multi Mod"
		{
			"ip"	"62.122.215.11:27015"
		}
		"Server #FUN"
		{
			"ip"	"46.174.52.17:27281"
		}
		"Server #MGE"
		{
			"ip"	"46.174.52.3:27290"
		}
		"Server #JAIL"
		{
			"ip"	"46.174.48.20:27203"
		}
	}
}
